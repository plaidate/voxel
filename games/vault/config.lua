-- Vault: tuning constants.

Config = {
    DT = 1 / 30,
    GRAVITY = 34,

    SPEED = 15,
    JUMP_VEL = 12,
    MAX_HP = 5,
    IFRAMES = 1.2,

    SWORD_R = 3.2,
    SWORD_CD = 0.4,
    BOMB_R = 3,
    START_BOMBS = 1,

    GRUB_SPEED = 6,
    AGGRO_R = 22,           -- grubs chase within this range

    BANNER_T = SMOKE_BUILD and 0.4 or 1.2,
}
